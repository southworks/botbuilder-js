{
  "name": "botbuilder-test-utils",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build2": "tsup src/index.ts --format cjs,esm --dts --clean --sourcemap",
    "build": "npm-run-all clean build:tsc build:tsup",
    "build:tsc": "npx tsc -b",
    "build:tsup": "tsup src/index.ts --format cjs,esm --sourcemap",
    "clean": "rimraf lib tsconfig.tsbuildinfo dist",
    "lint": "eslint . --ext .js,.ts"
  },
  "peerDependencies": {
    "jsonwebtoken": "*",
    "nock": "*",
    "sinon": "*",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.8",
    "@types/node-forge": "^1.3.11",
    "tsup": "^8.3.0"
  },
  "dependencies": {
    "nanoid": "^5.0.7",
    "node-forge": "^1.3.1",
    "zod": "^3.23.8"
  }
}
